# Nmap-Script
# Nmap Port Scanner

Scans all ports and detects services on a target.

## Usage

```bash
chmod +x nmap_scan.sh
sudo ./nmap_scan.sh 192.168.1.1
```

## Output

Creates `nmap_scans/` with two files:
- All ports scan
- Service details

Takes 10-30 min. Only scan systems you own.
